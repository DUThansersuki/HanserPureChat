# Invalid candidate build

This run accidentally used the untouched production database as its source. It
therefore contains zero reviewed Style rows and must not be used for evaluation
or release. The corrected rebuild is `persona_v2_candidate_2026-09-08_004`.
