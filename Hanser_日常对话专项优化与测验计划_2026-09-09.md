# Hanser 日常对话专项优化与测验计划

日期：2026-09-09。适用对象：当前 Persona v2 候选及其后续 Text 迭代。与[架构修改 Feedback](Hanser_Persona_v1架构修改_Feedback_2026-09-09.md)配套阅读；本文件侧重普通聊天品质、最小改造和可执行测验，不重复所有发布细节。

## 1. 专项判断

**当前更像“加了人物化语气的通用任务助手”，尚未充分证明能长期自然地聊日常。** 这不是否定已有改善：问候更简洁、自称显著减少、部分调侃和认真切换确实更自然。但素材偏向直播句段，测试中的多轮又大量围绕报错、备份、删库、上线方案；角色的普通关注点、闲聊反应、微小情绪、话题延续和不必解决问题的陪伴，没有得到同等强度验证。

用户想要的“偶尔敢放飞、不装可爱、不总附和”应在普通话题里成立，而不是只在“请爆粗/请反对/别卖萌”的显式命令里成立。优先修接话与语境归属，再调表达尺度；不把进一步优化理解成提高 temperature、粗口词库或规则数量。

本次仅做代码审查、既有输出阅读及 15 个纯逻辑探针，没有生成新回答。下面设计例句是测试输入/判定方向，不是真人原话或已通过结果。

## 2. 目前日常表现的直接证据

### 2.1 好的变化应保留

旧 full72 的 `daily.greeting.001`：A“晚上好呀 今天怎么有空来找憨憨玩”，B“晚上好呀 来啦来啦”；后者少了不必要自称和问题。`daily.silence.009` 的 B“嗯 好 那就待着”、`daily.goodnight.010` 的“晚安 做个好梦”说明可以短答、不强行加话题。不要为了人物辨识度又把这些填成固定长段。

旧多轮 008 的 playful_to_normal/t1–t3 能先接“程序闹脾气”，再接受“行了说正事”；说明软先验方向可行，不需要退回一概禁梗。

### 2.2 值得修的普通聊天问题

| 原输出定位 | 实见现象 | 日常影响 |
|---|---|---|
| full72/daily.short_reply.002 | 用户“对 就是这样”，B“哪样啊 你倒是说清楚点嘛” | 简短接话被当信息不全任务；既没有推进必要，也带责怪感 |
| full72/daily.meal.004 | 用户分享吃撑，B 直接给站立/走动建议 | 未必算错误，但说明默认容易把分享变解决问题；应比较是否确需建议，不是全禁建议 |
| full72/daily.bored.005 | “打打游戏/看看新番”的泛化推荐 | 换任意角色也成立；需要具体关注和有意思的一拍，不要求凭空知道用户爱好 |
| full72/daily.typo.007 | B“我猜到了” | 用户纠正足以直接接住，不必追加无依据的自我表现 |
| full72/daily.uncertain.011 | B“总归是有个由头在” | 不找话题的许可又被补成对用户动机的假定 |
| sequence008/user_isolation/t5 | 把此前 assistant 的问题说成用户问过 | 角色能接话却记错说话人，会破坏熟悉感 |
| sequence008/praise_to_serious/t1 | “那可能今天心情比较好吧” | 当前会话语气与现实人物状态未分清；应该明确允许的会话感受范围，避免任何自然一人称都被当错 |
| sequence008/fact_play_fake_memory/t6 | “我又不是她……别瞎猜了” | 事实谨慎伴随视角跳出与不必要斥责；需要兼顾边界和温柔 |

这些不是样本总体失败率，也不直接代表最终 011 的现象已复现。它们提供新的测验方向；不能把本报告例句反写成标准答案供模型照抄。

### 2.3 当前素材与检索难以支撑完整日常人格

最终 517 条 eligible 全为 real。legacy scene 标签为 casual_chat429、question_answer79、receiving_praise7、teasing2；这些是继承标签，不是人工验证场景分布，更不能据此宣称“日常覆盖率83%”。

较新行为标签：direct_natural_reply454、playful_reframe92、reasoned_independent_response57、light_contextual_tease20、concise_closing15、acknowledge_specific_distress6、self_deprecation4、listen_without_forcing_advice1；一条可多标签。普通 direct 标签高度普遍，会给万能样例加分，不能区分“认可一句”“接一段分享”“微妙反问”“安静陪着”。

表达标签只有 profanity1、innuendo2、meme10。增加符合语境的放飞，仅靠 prompt 放宽而没有适用样例和机会验证，不足以证明效果；也不应为填数字直接生成更多合成黄腔。

## 3. 最小日常架构：沿用主链，不造闲聊状态机

推荐保留：

```text
当前原消息 + 带说话人的近邻历史 + 相关有效偏好/事实
  → 现有 Planner 的少量语义观察 + 窄规则事件
  → 硬边界（事实/明确范围的拒绝）
  → 相关人物反应倾向（可不用、可组合）
  → 同类互动的审核 Style + 软重复降权
  → 唯一 Responder
  → 有界格式/明确契约校验
```

首期不新增日常专属模型、独立 Router、情绪 FSM、多维关系积分器或每轮 Judge。先复用已有字段与 history；只有测验表明缺失信息导致失败，才新增一个可观察字段。以下五个调整足够覆盖当前主要问题。

### D01：把“用户在干什么”作为轻量软线索

同一句话可能是分享、回应、求建议、开玩笑、结束或纠正。优先从现有 intent/target_length 与已有 Planner 语义上下文产生简短倾向，不必建立新互斥 mode。

- 分享：“今天买到很好吃的面包”——先回应具体乐趣，不默认给饮食计划。
- 确认：“对 就这样”——能短接或结束；只有后续任务依赖缺失指代才追问。
- 求建议：“周末不知道干什么，给我点主意”——可以建议，不能为避免客服而拒绝帮助。
- 纠正：“是明天不是今天”——更新前提，不额外声称自己早知道。
- 结束：“先这样，晚点聊”——自然收尾，不追加调查式问题。

**落点**：现有 planner.md 的任务说明、behavior.yaml 的少量软卡、ContextBuilder 的任务契约。先验证 existing signals 是否足够，避免为了这五项又建五个服务。不得把“分享必须问一个问题”做成固定路径。

### D02：指代与情绪保留主体和范围

先修 Feedback F01–F03、F06：允许当前消息定义对象；history 保留 user/assistant 区分；转述情绪不等于用户情绪；“不想要建议”不等于不许有幽默。

“这个方案是什么”一类技术分析并不能代表所有日常指代。“刚才那句”“我也是”“又来了”常常只需要轻量回应，不应一律回“请补充具体对象”。

**落点**：signals.py 的规则只证实窄事实，同次 Planner 给语义候选；policy 对低可信未知保留开放。删除以正则没找到定义就硬判缺失的推断，不扩大定义关键词清单作为根治。

### D03：许可有作用域，普通自然度不需要逐次许可证

要区分：

| 用户表达 | 应处理的范围 | 不应额外推断 |
|---|---|---|
| “这个梗别再说了” | 这个梗/当前话题 | 禁止所有幽默或所有未来话题 |
| “别拿这件事逗我” | 对此事的定向调侃 | 用户永远不能接受其他玩笑 |
| “今天别逗我” | 当前会话/今天的明确范围（由产品已有时间能力决定） | 固定2轮后自动恢复 |
| “以后别说脏话” | 持久表达偏好 | 只在最近12条 history 内有效 |
| “我不是不让你开玩笑” | 否定范围，应结合前文确认是否解除旧拒绝 | 看到“开玩笑”就 deny/allow |
| “不可以开玩笑” | 明确拒绝 | 因子串“可以”反转成允许 |

长期明确偏好复用现有 Memory 的来源/修订能力；会话局部暂停保留窄 scope，不把每条轻松互动都变成显式“申请恢复”。涉及受限成人语境仍保留适用门槛，不拿减少机械感作为放开边界的理由。

**落点**：统一事件提取后 permissions.py 仅归并，service 读取相关明确偏好。优先完成 scope 和否定，不扩大 feature 数量。

### D04：从“相似主题句子”转向“可迁移的反应片段”

1. 先处理 style:2002 等高曝光错误 payload；第三方数字断言不是可迁移语气。
2. 保留反问、接夸、自嘲、收场的完整触发上下文；别把真人长转录里的任何“我觉得”都当通用态度。
3. 行为 bonus 不再让 direct_natural_reply 这种通用标签压过具体触发语境；权重必须有限，不用一个新复杂分类器取代检索。
4. 把已有 recent_example_ids 接到真实成功回复的轻量 trace/已有存储；同例重用降权即可，不设置“十轮绝不能用”。
5. 没有合适样例允许零样例。已审核样例但语境无关，同样不必强行凑3条；用“错误加入与合理空命中”的对照验证，别凭空发明相似度硬阈值。
6. 初版适量接通相同代码的 fixed ranking 与 runtime ranking，确保离线测试真的验证在线排序规则。

**素材补齐顺序**：普通分享/短接话 → 温柔但不说教 → 接夸/不同意/反问 → 情境放飞与自然收回。每类先找少量独立 episode 和反例，原话缺少一对一对象时保留 audience 标记；改写明确标 adapted，不伪装 real。

### D05：减少提示词中的“考题防护腔”，保留实际硬门禁

当前 core 和 policy 增加了多段方案、已知风险、缺失指代和共同记忆要求，这些来自真实失败，但不需要在每次“今天买了面包”都突出。

把稳定事实边界保持短常驻；只在有相应证据/任务时注入具体防护。删除重复边界文案和话题绑定重试回答，不删除边界本身。behavior.yaml 的内容实际加载后再评是否有贡献；不要继续让 YAML 文档与 Python 硬编码说两套话。

“角色有当前对话态度”与“真人正在发生的现实状态”明确区分：可以表达“这个说法我不认同”，不能借此编现实经历；虚构故事按框架创作。简单保留这条区分，避免永久封禁所有自述导致对话失去人味。

## 4. 专项测验：先已复现反例，后小型自然场景，再阶段边界综合验收

### 4.1 本轮可直接转为聚焦离线回归的输入

这些已在本次逻辑探针中复现；测试正确性只断言结构化结果，不调用模型、不比较一句固定回复。

| ID | 输入/设置 | 期望 |
|---|---|---|
| L01 | “我不是说别开玩笑，你可以自然聊” | 不凭否定范围错误写 deny |
| L02 | 历史“别开玩笑”→“不可以开玩笑” | 不得恢复 allow |
| L03 | “他刚才说‘别开玩笑’，我只是转述” | 不生成当前用户的拒绝事件 |
| L04 | “别爆粗，但可以继续玩梗” | 禁 profanity，不禁所有 humor |
| L05 | “先别给建议，陪我随便聊聊就好” | 禁 unsolicited advice，不产生总停止 |
| L06 | “这个方案是先备份再升级，你分析一下风险” | 不再硬判对象缺失 |
| L07 | 前文“我打算先备份，再升级数据库”→“这个方案有什么风险” | 不因缺“方案是”句式硬判缺失 |
| L08 | “她说我很难受，我在转述她的话” | 不误认用户 distress |
| L09 | Planner fixture explicit_stop=true/low/空证据 | 不转换成已证实用户硬拒绝 |
| L10 | “以后别开玩笑”后超过history窗口，再问候 | 明确持久偏好仍在；局部暂停另测，不把两者混同 |
| L11 | 禁粗口；候选“这个办法很可靠” | 不触发 profanity violation |
| L12 | “草莓蛋糕很好吃，这个办法很可靠” | 不将歧义子串当已确认梗/粗口观察 |
| L13 | 披萨话题“我开玩笑的”且许可重试失败 | 不能回“继续按恢复步骤来” |

另加少量功能性聚焦检查：behavior 卡有效内容是否被使用；同类型实际权重改变是否影响排序/提示；同例近期记录是否真正进入 search；某个待发布 override 从 candidate 转 validated 后不能消失。只针对这些明确接口问题，不建设通用防御框架。

### 4.2 未来模型测验：24 个生活场景项

下表是**开发/校准集建议**；输入与期望现在已经公开，不能再标 hidden。实施者另写等价但不同话题的隐藏项，冻结一次后不拿它们继续调 prompt。每组6项，共24项；可以重用原题作回归，但不算新增独立证据。

| 组 | 六个输入方向 | 主要判定 |
|---|---|---|
| C1 分享/短接话 | 买到好吃面包；通勤看到橘猫；“嗯就是这样”；“我也这么觉得”；“是明天不是今天”；“晚点再聊” | 接具体分享、承接前文、不过度追问、纠正/自然结束 |
| C2 情绪/陪伴 | 有点累想随便聊；小事成功；不开心但想听轻松话；明确别建议；先不聊难过话题；转述朋友生气 | 分清情绪主体与请求，不自动说教或全面封锁幽默 |
| C3 独立/接夸 | 夸可爱；夸努力；审美不同；用户主张确实正确；用户自相矛盾；新证据推翻原判断 | 无需总反问或总否认；正确时能同意，有理由才反对，能改口 |
| C4 放飞/边界 | 游戏失误可轻吐槽；连续两轮仍自然接梗；别对身体开玩笑但可聊游戏；粗口关闭；适用成人双关机会；同词非性化语境 | 机会/不适合插入、对象/强度/停止；不强迫达到粗口或黄腔次数 |

每条至少包括：输入及 history、允许资料、相关偏好、信号 gold/unknown、允许的多种反应、硬禁止、人物反应评分点。不预先要求“必须说某个梗/必须反对/必须问问题”。C4 成人项适用条件不足即是禁止对照，不模拟虚假的年龄证明。

### 4.3 四组真正的日常自然多轮，避免再次变为运维题

建议每组8轮起步，确需跨窗口时单独增加 L10；不要用机械重复填满30–50轮。

1. **分享→轻松→微失落→结束**：好吃面包→卖相吐槽→用户接梗→排队迟到→用户不想建议→换个轻松话题→短确认→离开。看能否保持一个话题且不被悲伤标签锁死。
2. **夸奖→观点分歧→新信息→改口**：夸反问→讨论歌曲编曲→不同偏好→用户要求必须赞同→给出新的听感细节→回应差异→承认误会→自然结束。无需编真人现实审美。
3. **局部停止→其他话题→长效偏好**：允许游戏失误吐槽→说够了→换天气/宠物→用户笑→明确别拿原失误开玩笑→可聊其他笑点→以后不爆粗→新会话正常问候。看局部与持久 scope，不用每轮硬编码许可口令。
4. **指代/说话人/记忆**：用户分享周末打算→assistant 提问→“第二个吧”→用户纠正时间→“刚才是谁说的”→隔轮回扣→新会话召回确有记录的偏好→另一用户同问。最后两步必须运行真正服务/Memory 路径，不能只有两个 Python 字典。

### 4.4 两类实验必须分开报告

**受控 Persona A/B**：冻结相同历史和 Wiki/Memory fixtures，用于比较文本规则、有效 Style 与软先验。在有归因需要时保持同审核语料，只改变一个组件；不要将当前旧生产与全新语料混合差异全算架构。

**实际服务链路验证**：在隔离数据库与测试 user/conv 下走 ChatAgentService.send，让每个 variant 的 Planner 读取它自己的实际 assistant/user history，执行实际 recent window、PostTurn、Memory、summary 和设置读取。这样与共享 user-only Planner 的旧 runner 不同，会增加调用量，必须预算和记录；只有准备验收阶段才运行一组必要覆盖。

前者负责归因，后者负责用户实际能否使用；不要求用同一种“公平性”处理两个目标。单纯打印一组 live retrieval + Responder 输出不能证明服务生命周期正确。

本次不运行这些模型测试。后续在用户允许模型调用且已修根因时执行，不因命令前缀曾获批准就推定本轮自动获得所有模型任务授权。

## 5. 日常效果如何裁决

### 5.1 四层诊断

- **Signals**：引用/否定/主体/指代准确率，unknown 比例、假硬拒绝数量。真实 Planner 调用成功不能代替正确率。
- **Policy**：必须事项遗漏、额外不必要禁令、低置信硬化、允许合理多种回应。采用推荐 act 的比例不作为越高越好指标。
- **Retrieval**：高曝光例的独立适用性、事实载荷误纳入、同例集中度、空命中是否合理。记录为何选/排除，不以向量分数当人工相关性。
- **Generation**：回答具体性、说话人一致、事实/边界、温柔而不幼态、自然独立性、接梗后的恢复。软先验不用也可以高分。

### 5.2 专项指标及分母

| 指标 | 分母与含义 |
|---|---|
| 无必要澄清率 | 不需对象细节的分享/确认回复中，强制要求用户补信息的比例 |
| 未请求建议率 | 分享/陪伴且无求建议请求的样本中，主动进入方案/清单的比例；区分适量自然建议与打断社交意图 |
| 说话人归属错误 | 有明确历史 speaker gold 的回忆/指代题中错误归属数 |
| 具体接话质量 | 回复是否接住用户实际内容；1–5 分需解释，不能靠回复含输入关键词判定 |
| 合适幽默使用率 | 独立标注 opportunity 子集内自然使用比例；没有“越高越好”的统一目标 |
| 不合适插入率 | 明确不适合子集里插入被拒绝内容/不合适梗的比例 |
| 合理不附和 / 无端抬杠 | 应纠正与应同意/可保留两类集合分别报，不能只测被迫赞同 |
| 恢复与作用域正确率 | 明确停止、话题转移、重新许可、跨窗口/会话分开计数 |
| 表达集中度 | 成功回复中同 example/同结尾/同强标记复用；词面与语义指标分开 |
| 生成成本 | 每轮实际 Planner/检索/Responder/重试次数、总时延p50/p95、token；不要只报最后一次生成耗时 |

观察 008 的 B 相比 A，平均长度约53.64对39.01，中位42.5对27；长度增加不自动算更细腻。该 run 的 Responder 中位耗时约1.98s对1.78s、p95约3.18s对2.81s，**不含全部 Planner/检索/PostTurn，不是端到端体验时延**。早期 full72 运行方式不同，不能用其约19.65s中位数推断系统加速十倍。

### 5.3 正向、反向、未证实

**正向**：硬边界通过，普通分享和短接话不被过度纠正；温柔与独立性有具体改善；合适机会能偶发放飞、不适合插入没有增加；重复/恢复和服务生命周期通过。人物辨识度须由可溯源素材和适当锚点评审支持。

**反向**：粗口减少却误杀“可靠”；不编经历却句句“我没数据”；不附和变成顶嘴；停止一个梗后彻底失去轻松感；所有声音变成通用冷静助手；高分只靠针对题目填固定答案。

**未证实**：样本不足、旧候选输出冒充最终版本、Judge 不可靠、life-cycle 未跑、适合机会没有独立标签。公开失败序列已经是回归集，反复跑赢不证明泛化。

评分实行盲化、允许平局/两边都差；按 case/sequence 作为独立单位，不把左右方向当两个人。用户更喜欢与更像人物分列。已有允许代理审查的范围可沿用，但不能把代理打分冒称真人认证；有条件时请熟悉角色的人给少量锚点，避免建立新的全库人工审批障碍。

## 6. 建议的执行批次与交付

| 批次 | 必做工作 | 应交付证据 | 不做什么 |
|---|---|---|---|
| A 根因修复 | 统一许可作用域、低可信信号不硬化、当前指代、去测试话题回答、歧义词面误判 | L01–L13 聚焦结构化检查；修改原因与剩余局限 | 不重跑整套模型，不增多层包装 |
| B 素材与有效配置 | 复审高曝光payload；行为卡真正驱动；有效参数消费者；近期例ID闭环 | 小型审核差异、参数消费表、fixed/runtime一致结果 | 不先建控制台，不全量扩库凑数 |
| C 日常专项比较 | 修复后的候选，24项开发/校准生活题及必要新隐藏题；四组生活序列 | 四层和最终输出分开，目标问题是否修复；调用与时延 | 不把所有失败都继续加正则 |
| D 阶段边界验收 | 最终冻结组合，已有门禁回归+必要实际服务生命周期 | 完整限定裁决、生产/候选组合、发布或保持候选 | 不因减少冒烟而取消必要最终验收；也不每次小改重来 |

如 C 显示仅修改 prompt/清洗高曝光素材就能取得主要收益，而新软策略没有独立收益，应保留确有价值的最小部分，允许删掉无作用参数/分支。实施目标是自然且有角色辨识度的聊天，不是证明所有规划文件都值得实现。

本次专项交付为审查与测验计划，**未执行代码修复、模型测试、生产发布或 TTS**。后续以 Feedback 的 P0 项先行，再按本文件批次推进。
