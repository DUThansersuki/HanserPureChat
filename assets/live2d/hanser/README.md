# Hanser Live2D assets

No Live2D model is present or licensed in this repository. When one is
provided, record its source and permitted use, probe actual parameter ranges,
then create a reviewed rig profile. Do not assume generic `ParamMouth*` names.
The runtime keeps `rig_profile.candidate.yml` disabled until that calibration
is complete.
