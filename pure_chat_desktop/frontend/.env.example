# The Electron shell assigns these values at runtime. They are only needed when
# starting the frontend directly during development.
HANSER_API_BASE_URL=http://127.0.0.1:18765
HANSER_USER_ID=local-user
HANSER_DESKTOP_TOKEN=replace-with-a-random-local-token
