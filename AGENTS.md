# Codex Agent 项目记忆

适用范围：`H:\HanserAgent` 全项目及其子目录。

## 工程实现偏好

- 在项目工程中减少防御性编程的使用。只针对已复现故障、明确接口边界或项目规范中的硬门禁增加防护；优先修复根因，避免推测性兜底、重复校验、宽泛异常吞没与多层包装。
- 大幅减少哈希校验频率。仅在候选包/发布清单、冻结评估输入、索引代际和回滚兼容等确需完整性证明的阶段边界计算或复核，不在普通编辑后重复计算。
- 大幅减少冒烟测试频率。优先执行与改动直接相关的聚焦测试；只有相关入口、依赖、配置或阶段边界变化时才运行冒烟测试，候选冻结前做一次必要综合验证。
- 上述偏好不取消 `Hanser_Persona_专项架构与执行规范.md` 明确列出的 Persona 硬门禁、来源追溯、冻结评估和回滚兼容要求。

## 语音素材来源

- `H:\voice_process\without_bgm_clarified` 下的语音切片及同名对照文本均由用户人工核查，属于高可用来源；运行时副本必须放在项目内 `assets/voices/hanser`，并在 manifest 保留来源追溯。
