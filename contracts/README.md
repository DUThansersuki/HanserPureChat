# Generated extension contracts

Files in `generated/` are exported from the validated Python contract models
by `scripts/export_performance_schemas.py`. Regenerate only when a contract
revision changes. Runtime startup does not generate or mutate these files.
